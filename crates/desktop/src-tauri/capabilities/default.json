{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main Cobblestone desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
