[workspace]
members  = ["crates/core", "crates/cli"]
# crates/desktop компилируется в WASM (trunk) и не входит в основной workspace
exclude  = ["crates/desktop"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"
